<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Console Appender Configuration -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <!-- Pattern specifies: Date [Thread] Level LoggerName - Message -->
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Package level logger configured to DEBUG for our source classes -->
    <logger name="com.cognizant.springlearn" level="DEBUG" additivity="false">
        <appender-ref ref="CONSOLE"/>
    </logger>

    <!-- Root logger configured to INFO for general logging -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>

</configuration>
